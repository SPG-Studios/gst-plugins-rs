[package]
name = "gst-plugin-image"
edition.workspace = true
version.workspace = true
repository.workspace = true
rust-version.workspace = true
authors = ["Amyspark <amy@centricular.com>"]
license = "MPL-2.0"
description = "GStreamer plugin based on image-rs"

[dependencies]
gst.workspace = true
gst-base.workspace = true
gst-video.workspace = true
image = { package = "image", version = "0.25.9", default-features = false, features = ["rayon", "nasm"] }
num-rational = { version = "0.4", default-features = false, features = [] }

[dev-dependencies]
gst-check.workspace = true

[lib]
name = "gstimagers"
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[build-dependencies]
gst-plugin-version-helper.workspace = true

[features]
static = []
capi = []
doc = ["gst/v1_26"]
default = ["bmp", "gif", "ico", "jpeg", "pnm", "qoi", "tga", "tiff"]
avif = ["image/avif"]
bmp = ["image/bmp"]
dds = ["image/dds"]
exr = ["image/exr"]
ff = ["image/ff"]
gif = ["image/gif"]
hdr = ["image/hdr"]
ico = ["image/ico"]
jpeg = ["image/jpeg"]
png = ["image/png"]
pnm = ["image/pnm"]
qoi = ["image/qoi"]
tga = ["image/tga"]
tiff = ["image/tiff"]
webp = ["image/webp"]

[package.metadata.capi]
min_version = "0.9.21"

[package.metadata.capi.header]
enabled = false

[package.metadata.capi.library]
install_subdir = "gstreamer-1.0"
versioning = false
import_library = false

[package.metadata.capi.pkg_config]
requires_private = "gstreamer-1.0, gstreamer-base-1.0, gstreamer-video-1.0"
